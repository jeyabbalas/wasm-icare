/**
 * Browser entry. In Phase 7 this wires the module-worker bootstrap and
 * self-hosted / pinned-CDN Pyodide asset resolution into `loadICARE`.
 * For now it re-exports the shared surface.
 */
export * from './index';
